import React from "react";

/* This page show Coding-Hubs page, or at least give some sort of redirect.
it should still be clear that a club is not found. */

function PageNotFound() {
  return <div>page is not found</div>;
}

export default PageNotFound;
